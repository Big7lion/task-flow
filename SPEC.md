我需要实现一个可视化任务流配置模块：
这个模块共三个部分：使用antv-x6渲染的任务流渲染容器，预设节点面板，使用lil-gui绘制的节点配置项菜单

模块高度可配置并且动态适应，默认宽高为1200x800

模块的数据格式为在antv-x6的标准数据格式上，每个节点数据上增加了名为config的对象列表，这个字段用于保存这个节点的可配置项列表和实际值，用于作为业务的自定义数据内容；其中可配置项主要是: label属性名, value配置值和type值类型（string/number等等，与lil-gui可用的数据类型做一些匹配），以及一些可供lil-gui使用的配置字段

### antv-x6渲染容器

1. antv-x6容器需要支持拖拽、缩放，需要支持子图
2. antv-x6容器需要支持各节点：可框选、复制、粘贴、按删除键删除节点、可点击选中
3. antv-x6容器铺满整个模块，动态适应模块宽高，并做好throttle
4. antv-x6模块使用默认主题色
5. 需要支持给节点之间添加边，这个边的绘制需要是柔和圆角的90°拐弯线，并且有箭头指向
6. 需要自动保存当前编辑中的内容到localstorage，并且在下次页面时，自动加载

### 预设节点面板模块

1. 需要支持从预设节点面板中，拖拽预设节点到渲染容器中，放置后自动在渲染容器在对应的位置增加节点
2. 预设节点可配置，配置项可以是一个antv-x6的json格式对象
3. 渲染容器中框选了一些节点后，按ctrl+s，则将这些节点作为一个新的预设节点加入到列表中
4. 支持手动增加一个预设节点，手动增加需点击一个添加按钮，点击后弹出一个modal，modal里包含一个JSON编辑器，用户输入完成后通过保存按钮确认添加；这个编辑器支持自动保存草稿到localstorage，在未确认主动关闭的情况下，再次打开modal会自动填入操作内容
5. 支持删除预设节点
6. 预设节点列表可以通过localstorage存储，不影响性能的情况下及时更新

### 节点配置项菜单

配置项菜单使用lil-gui绘制，该菜单可展开和收起

菜单内常驻四个操作：1. 保存 2. 校验 3. 导出为图片 4. 清空页面
即使菜单收起也可见；前两个按钮的回调事件里暂时不做任何处理，只打个log，第三个按钮需调用antv-x6的内容来导出图片，第四个按钮则在弹窗让用户确认后清空容器内容

当渲染容器中选中了单个节点后：自动展开菜单，并按照节点的config可配置内容，展示节点配置项
当渲染容器中框选了多个节点后：节点配置项清空，收起菜单
当用户修改了节点配置后：及时更新数据，并同步到渲染模块